import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Outstock E-Commerce Website',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
